local options = {
  ensure_installed = { "lua", "c", "cpp", "python", "ocaml" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
